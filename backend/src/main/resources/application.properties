# Spring Boot application properties
# Server port (optional, defaults to 8080)
# server.port=8080

# Static resources location (defaults are classpath:/META-INF/resources/, classpath:/resources/, classpath:/static/, classpath:/public/)
# No specific configuration needed here if we place frontend build output in src/main/resources/static
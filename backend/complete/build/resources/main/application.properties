# Database Configuration
# Development: H2 in-memory database
spring.datasource.url=jdbc:h2:mem:kidsinmotiondb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Production: PostgreSQL (uncomment for production)
# spring.datasource.url=jdbc:postgresql://localhost:5432/kidsinmotion
# spring.datasource.username=${DB_USERNAME:kidsinmotion}
# spring.datasource.password=${DB_PASSWORD:password}
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=false

# Firebase Configuration
# The Firebase Admin SDK will use the service account key or GOOGLE_APPLICATION_CREDENTIALS
# No additional configuration needed here

# Server Port (Optional - defaults to 8080)
# server.port=8080

# Allow CORS for frontend development (adjust origin as needed)
# In production, configure this more restrictively
cors.allowed.origins=http://localhost:3000
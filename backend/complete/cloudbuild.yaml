steps:
  # Build the application
  - name: 'gradle:7.6.1-jdk17'
    entrypoint: 'gradle'
    args: ['build', '-x', 'test']
    dir: 'backend/complete'

  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/$PROJECT_ID/kidsinmotion-backend:latest',
      '-t', 'gcr.io/$PROJECT_ID/kidsinmotion-backend:$BUILD_ID',
      'backend/complete'
    ]

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/kidsinmotion-backend:latest']

  # Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'run', 'deploy', 'kidsinmotion-website',
      '--image', 'gcr.io/$PROJECT_ID/kidsinmotion-backend:latest',
      '--region', 'us-east4',
      '--platform', 'managed',
      '--allow-unauthenticated',
      '--port', '8080',
      '--memory', '2Gi',
      '--cpu', '2',
      '--timeout', '900',
      '--set-env-vars',
      'SPRING_PROFILES_ACTIVE=development,CORS_ALLOWED_ORIGINS=https://kidsinmotionpa.org,https://kids-in-motion-website-b1c09.web.app,https://kids-in-motion-website-b1c09.firebaseapp.com'
    ]

images:
  - 'gcr.io/$PROJECT_ID/kidsinmotion-backend:latest'